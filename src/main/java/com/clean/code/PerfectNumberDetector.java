package com.clean.code;

public class PerfectNumberDetector {
	
	public String determinePerfectNumber(int number){				
		return (number == 0) ? "Input Number is not a Perfect Number." : null;
	}
}
